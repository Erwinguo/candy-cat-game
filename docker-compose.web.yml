services:
  web:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
    container_name: tangdou-web
    restart: unless-stopped
    environment:
      TANGDOU_API_BASE: ${TANGDOU_API_BASE:-https://www.gzywl.cn}
    ports:
      - "127.0.0.1:18080:80"
